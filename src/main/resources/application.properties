# spring.application.name=FixuraBackend
# server.port=8080
# spring.datasource.url=jdbc:postgresql://dpg-cs5lhbd6l47c73f5m9jg-a.oregon-postgres.render.com:5432/fixura_db
# spring.datasource.username=team_fixura
# spring.datasource.password=SUeNIyfMsvifLFhVWI88IL3jyfPNmIqU
# spring.datasource.driver-class-name=org.postgresql.Driver
# logging.level.org.springframework.jdbc.core=DEBUG
# logging.level.org.hibernate.SQL=DEBUG
# jwt.secret=FDKOoIzN85UttqixVC6eCmI+lT1mgDjtnV8D0CfloY4=
# github.secret=GITHUB_TOKEN
# jwt.email.secret=A3!gk8#LmPz9$wR5qX@e2^FnTj6*bVd1&cQ7YhZ4
# time.email.expiration=300000

# #Configuracion para el envio de correos
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${MAIL_USERNAME}
# spring.mail.password=${MAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

spring.application.name=FixuraBackend
server.port=8080

# Base de datos
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Logging
logging.level.org.springframework.jdbc.core=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# JWT secrets
jwt.secret=${JWT_SECRET}
jwt.email.secret=${JWT_EMAIL_SECRET}

# Configuración de correos
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Otros
github.secret=${GITHUB_SECRET}
time.email.expiration=300000